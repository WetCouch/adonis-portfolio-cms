@layout('site.layout.app')
@section('content')
@set('title', 'Blog')

<section class="min-height flex flex-wrap">
  @each(post in posts)
    <div class="col-4">
      @include('site.components.post-card')
    </div>
  @else
    <h1>More articles will be added soon...</h1>
  @endeach
</section>

@endsection
